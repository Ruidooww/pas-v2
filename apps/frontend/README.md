# PAS Frontend

Vite React SPA placeholder for the V0 implementation.

The frontend container should serve the built SPA through Nginx and proxy `/api` to `pas-backend`.
It must not hold RAGFlow, CRM, or LLM credentials.

The initial screen is an operational shell for the V0 modules, not a marketing landing page.
