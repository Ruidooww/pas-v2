{
  "name": "pas-v2",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.5.3",
  "scripts": {
    "build": "pnpm -r build",
    "ci:images": "node scripts/build-images.mjs",
    "compose:config": "node scripts/verify-compose.mjs",
    "lint": "pnpm -r lint",
    "smoke:local": "node scripts/smoke-local-menu.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22 <23"
  }
}
